# === Secrets — NE JAMAIS committer ===
.env
.env.*
!.env.example

# === Dépendances ===
node_modules/

# === Données locales de l'app organisation-cours ===
# Règle héritée : ne jamais écraser NI committer ces données (RGPD).
apps/organisation-cours/data/

# Retours et audits : contient des captures d'écran de tests en conditions
# réelles (noms, emplois du temps, identifiants de compte). Le dépôt est
# PUBLIC — ce dossier reste local, décision de Martin (02/08/2026).
apps/organisation-cours/retours/

# === Réglages locaux Claude Code (propres à chaque poste) ===
.claude/settings.local.json

# === Fichiers système / éditeurs ===
.DS_Store
Thumbs.db
.vscode/
.idea/

# === Logs ===
*.log
npm-debug.log*
