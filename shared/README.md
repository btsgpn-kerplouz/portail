# shared/

Ressources **communes à toutes les apps** :

- design system CSS (styles partagés),
- helpers JavaScript,
- client Supabase (connexion à la base, clé publique `anon` uniquement).

But : éviter de dupliquer le même code dans chaque app.
